/*
 * parallel_copy_aggregate.cpp
 *
 *      Author: ke fan
 */

#include "../parallel_RA_inc.h"

bool parallel_copy_aggregate::local_aggregate(
		u32 buckets,
		shmap_relation* input0,
		shmap_relation* input1,
		relation* output)
{

	return true;
}
